import styled from 'styled-components';
import {colors} from 'frontend/src/styles';

export const Root = styled.nav`
  background-color: ${colors.common.white};
  border-bottom: 1px solid ${colors.common.border};
`;
